{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "run-s -ls lint:*",
    "lint:staged": "burst lint --fix --staged ||:",
    "lint-project": "burst lint --fix",
    "postinstall": "is-ci || (cd .. && husky install)"
  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@burst/ts-config": "0.0.4",
    "@types/node": "^14.14.28",
    "@types/react": "^17.0.2",
    "husky": "^5.0.9",
    "is-ci": "^2.0.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.1.5"
  }
}
